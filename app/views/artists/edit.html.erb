<h2>Edit 
<% if @artist.name.last == "s"  %> 
<%= @artist.name %>' 
<% else %> 
<%= @artist.name %>'s 
<% end %> 
Information Here</h2>

<%= form_for(@artist) do |f| %>
    <%= f.label :name %> :
    <%= f.text_field :name %>
    <%= f.label :bio %> :
    <%= f.text_field :bio %>
    <%= f.submit "Update #{@artist.name}'s info" %>
<% end %>
  


